# 装备党 iOS App 开发文档

## 项目概述
“装备党”是一款基于 SwiftUI 的 iOS 应用，核心价值是帮助用户记录并洞察自己购买的各类“装备”在时间维度上的真实投入。应用通过计算装备的日均成本、按场景组织装备，展示长期持有带来的成本摊薄效果，并支持多语言与 iCloud 同步。

## 核心目标
- **装备资产管理**：记录装备的购买信息、当前状态及所属场景，聚合展示投入情况。
- **成本洞察**：根据购买价格与持有天数自动计算日均成本，使用户感知长期投入的摊薄效果。
- **场景化浏览**：以场景为单位展示装备集合（如运动、游戏等），支持查看场景总投入与单件装备详情。
- **多语言支持**：首期支持简体中文、繁体中文、英文，可在设置页切换。
- **iCloud 同步**：通过 Core Data + CloudKit 保持多设备数据一致。

## 功能模块
### 1. 装备与场景管理
- 创建、编辑、删除装备与场景。
- 装备字段：名称、购买日期、购买价格、所属场景、备注、图片、是否在使用等。
- 计算属性：持有天数、日均成本（购买价格 ÷ 持有天数）。

### 2. 场景视图
- 首页展示场景卡片或列表，显示场景内装备数量、总投入、综合日均成本。
- 场景详情列出装备清单，支持排序（按日均成本、购买时间等）。

### 3. 装备详情
- 展示装备的详细信息与日均成本曲线（首期可用简单统计代替）。
- 支持编辑装备信息、更新状态、上传或更换图片。

### 4. 设置页
- 语言切换（简体中文 / 繁体中文 / 英文）。
- iCloud 同步状态（最近同步时间、是否启用）。
- 后续可扩展导入导出、隐私与通知设置。

## 技术方案
### 架构模式
- **MVVM + SwiftUI**：SwiftUI 构建界面，ViewModel 管理状态与业务逻辑，Model 层封装 Core Data 实体和计算。
- **Core Data + CloudKit**：使用 `NSPersistentCloudKitContainer` 提供本地持久化与 iCloud 同步，处理冲突策略（以最新更新时间为准）。

### 数据模型
- `Scene`：字段包括 `id`、`name`、`desc`、`icon`，与多条 `Equipment` 关联（1:N）。
- `Equipment`：字段包括 `id`、`name`、`purchaseDate`、`purchasePrice`、`isActive`、`notes`、`image`、`scene` 等。
- 可选扩展 `EquipmentUsage` 用于记录使用状态变更或停用时间。
- 在 Swift 代码中扩展 `Equipment` 提供 `heldDays`、`dailyCost` 计算属性。

### 多语言
- 使用 `Localizable.strings`（简体中文、繁体中文、英文）管理 UI 文案。
- 结合 `AppStorage` 或自定义环境对象实现应用内语言切换，必要时引入 `Bundle.main.path(forResource:ofType:)` 动态加载字符串表。

### 同步与持久化
- 配置 `NSPersistentCloudKitContainer` 与 CloudKit 容器同步。
- 设计后台合并策略，采用 `mergeByPropertyObjectTrump` 或 `mergeByPropertyStoreTrump` 基于更新时间解决冲突。
- 在设置页展示同步状态，监听 `NSPersistentStoreRemoteChangeNotification` 以更新 UI。

### UI 结构建议
1. **首页（场景列表）**：使用 `NavigationStack` + `List`/`ScrollView` 展示场景卡片。
2. **场景详情页**：展示装备列表，支持筛选与排序。
3. **装备详情页**：显示装备信息、日均成本、操作按钮。
4. **设置页**：使用 `Form` 展示语言切换与同步信息。
5. 通过 `TabView` 或 `NavigationSplitView` 组织主流程。

## 分步开发计划
### 里程碑 1：数据层基础（当前迭代）
1. 建立 Core Data 模型（Scene、Equipment 实体及关联），配置 CloudKit 容器标识。
2. 编写 `PersistenceController`，封装 `NSPersistentCloudKitContainer` 初始化、预览数据注入与保存方法。
3. 提供 `Scene`、`Equipment` 的 Core Data 扩展，补充计算属性（持有天数、日均成本、场景聚合成本）。
4. 搭建 SwiftUI 与 Core Data 的基础桥接（注入 `managedObjectContext`、提供预览数据）。

### 里程碑 2：界面骨架
1. 构建 TabView/Navigation 栈，串联首页、场景详情、设置页。
2. 实现场景列表、场景详情、装备详情的初版界面与基本增删改查操作。
3. 引入 ViewModel，将核心业务逻辑与 UI 解耦。

### 里程碑 3：多语言与同步反馈
1. 准备三种语言的 `Localizable.strings`，覆盖现有 UI 文案。
2. 实现应用内语言切换机制，并确保视图实时响应。
3. 在设置页展示同步状态、最近同步时间、错误提示。

### 里程碑 4：体验优化与扩展
1. 增强数据可视化（如日均成本趋势、场景投入对比）。
2. 支持装备图片管理、导入导出和提醒功能。
3. 优化无障碍、性能与测试覆盖率。

## 开发阶段与里程碑
1. **基础数据层搭建**
   - 建立 Core Data 模型与实体关系。
   - 实现 `NSPersistentCloudKitContainer` 配置，验证本地增删改查与同步。
   - 编写单元测试验证日均成本等计算逻辑。

2. **界面骨架实现**
   - 构建 SwiftUI 导航与视图结构。
   - 将 Core Data 数据绑定到 SwiftUI，支持新增/编辑/删除。
   - 完成场景列表、场景详情、装备详情、设置页的基础 UI。

3. **多语言与同步可视化**
   - 配置多语言字符串资源，并在 UI 中应用。
   - 设置页实现语言切换与同步状态展示。
   - 监听同步通知，完善错误处理与提示。

4. **优化与扩展（后续迭代）**
   - 增加数据可视化（例如日均成本趋势）。
   - 实现导入导出、使用提醒等高级功能。
   - 优化性能、可访问性与界面细节。

## 测试与质量保证
- 单元测试：覆盖日均成本计算、数据模型逻辑。
- UI 测试：验证关键用户流程（新增装备、查看场景、语言切换）。
- 同步测试：在多台设备或模拟器上验证 CloudKit 同步准确性。
- 本地化检查：确保不同语言下文案完整、布局合理。

## 工程与协作规范
- 使用 Git 进行版本控制，采用分支开发和代码审查流程。
- PR 需包含功能概述、测试记录与截图（如涉及 UI 改动）。
- 使用 SwiftLint（可选）保持代码风格一致。

---
该文档用于快速了解项目背景、核心需求、技术方案与实施步骤，作为后续开发沟通与协作的基线。根据后续需求变更，可持续补充与更新。
